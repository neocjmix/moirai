name: IP-011 A4 Collection frame timeline

on:
  pull_request:
    paths:
      - ".github/workflows/ip011-a4-collection-timeline.yml"
      - "scripts/ip011-a4-collection-timeline.ts"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  collection-timeline:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PUBLIC_INTEGRATION_URL: >-
        ${{ vars.PUBLIC_INTEGRATION_URL || 'https://moirai-production-8ed1.up.railway.app' }}
      EXPECTED_COMMIT_SHA: ${{ github.event.pull_request.base.sha }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Verify deployed base SHA and public smoke
        if: github.event_name == 'pull_request'
        run: pnpm smoke
      - run: pnpm exec playwright install --with-deps webkit
      - name: Timestamp cold and warm Collection graph frames
        run: |
          set -o pipefail
          node --import tsx scripts/ip011-a4-collection-timeline.ts \
            | tee /tmp/ip011-a4-collection-timeline.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ip011-a4-collection-timeline
          path: /tmp/ip011-a4-collection-timeline.json
          retention-days: 14
