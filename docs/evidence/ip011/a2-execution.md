# IP-011 A2 execution evidence

A1 shipped in [PR #130](https://github.com/neocjmix/moirai/pull/130), merge `1b837bf7457090da2d4225219fa72265865ec8bf`. Railway web/API/worker all SUCCESS at that commit. API readiness reports the same SHA; the new policy route returns 401 to unauthenticated callers. Live World still reports 30/30/30 ready. CI exercises authenticated HTTP/MCP policy equality and CLI policy-envelope preservation. This session's connected Live tool catalog predates the new method; a production authenticated `authoring_policy_get` call has not been exercised through that cached catalog. The source artifact and deployed commit are pinned; refreshing the app catalog is needed for a fresh authoring session.

## Owner-full inventory

[a2-owner-inventory.json](a2-owner-inventory.json) is an owner database read, not a World-scoped export. It used a repeatable-read, read-only transaction with a 20s statement timeout in the existing worker execution environment. No credentials or body/origin text were printed. Temporary worker startup instrumentation was removed from service configuration after collecting the result.

The installation contains one World, at revision 30; migrations 001–009 are actually applied. Counts: 127 active Events, 6 active Canons, 153 active Event memberships, 429 total Relations (415 active, 14 withdrawn), 491 Relation memberships (477 active, 14 withdrawn), 160 active Narratives, 1474 Change Operations and 30 Change Sets/Revisions. All Narratives use ko. There are no Subject handles/members and no correspondence tables. Thus no handle redirect migration or multi-locale conversion is needed for this installation. This does not remove the target invariant or future tests.

The public snapshot omitted the 14 withdrawn Relation rows and 14 withdrawn memberships. A `.moirai` content export alone is therefore insufficient as the rollback backup. Backup must include all table/history rows and sequence state.

## Rehearsal tooling

The operator-only `scripts/ip011-backup-rehearsal.ts backup-and-rehearse` captures the exact v4 table inventory in a read-only transaction, keeps PostgreSQL JSON text intact (including large numbers and microsecond timestamps), encrypts with AES-256-GCM before writing an immutable operational backup object, reads/decrypts it back, and restores into a newly created `ip011_rehearsal_<random>` database. It never restores over an existing database or writes a Publication pointer. The source's full row/sequence digest is checked again after rehearsal.

`IP011_BACKUP_KEY` is an independent 32-byte secret supplied through the worker's secret variables, never committed, logged, returned, or stored with the ciphertext. Object storage receives only authenticated ciphertext under `operational-backups/ip011/`, never plaintext private Change Set origins. This is a v4 migration backup format, not a new public export contract. Unknown tables or schema versions stop the operation. Columns/defaults, constraints, indexes, triggers, table ACL/RLS policies, public function definitions and views are fingerprinted and compared before restoring rows; matching migration ledger names alone do not prove absence of schema drift. Rehearsal DB names are recorded for subsequent v5 tests and explicit cleanup.

A PostgreSQL integration test verifies restore fidelity, numeric/date precision, source immutability, sequence state and refusal to overwrite an existing database. Local crypto tests verify wrong-key and ciphertext-tamper rejection. PR #131 merged at `f0331129f1a84d0bd3adf20ba9e63e0c262fb964` after all [CI checks](https://github.com/neocjmix/moirai/actions/runs/35784846155) passed. The actual production snapshot was encrypted, stored, read back and restored into an isolated database with identical schema/data/sequence digest and an unchanged source digest. See [a2-backup-result.json](a2-backup-result.json). Temporary worker startup configuration was restored. The clone is retained for v5 migration tests; no service points at it and its outbox is not processed.

## Content mapping findings

Revision 30 has 119 distinct Narrative owners and 160 Narrative rows. Preserving one current Narrative ID per owner, adding the 14 missing owners and retiring 41 superseded IDs yields 133 active target Narratives and 174 total retained IDs. One owner (`단종 폐위와 몰락의 과정`) has only a summary Narrative; its ID can be explicitly retained as the target owner Narrative rather than inventing a duplicate primary ID. Retired bodies and all citations remain in history and the preservation manifest; notes become components of the retained owner's Narrative.

The eight reign/founding Composite summaries are process/precision commentary and need reader-facing replacement based on their actual children and source Narratives. Four additional Event owners and two Collection owners lack any Narrative. These require reviewed prose, not mechanical concatenation or arbitrary first-Canon selection.

An offline v4-validator experiment over the revision-30 snapshot assigned all 127 Events and 415 active Relations to one synthetic scope and enabled all Time Systems there. The union passed with zero warnings. This is preliminary evidence that current temporal/contains assertions can coexist; the final v5 World invariant suite and a real restored-database migration remain mandatory.

## Remaining A2 gates

Narrative preservation manifest is now materialized and passes offline revision-30 validation ([input and editorial decisions](../../../data/migrations/ip011/README.md)); v5 canonical/contract/API/UI/MCP/export/tests transition remains; migration on the isolated restored copy; old Revision/export fidelity; World-level invariant and scenario tests. No production schema/content cutover has been executed. A3 remains gated on all of these.

## Adversarial review corrections

A restore through `jsonb_populate_recordset` could silently discard a manually added column if only migration names were compared. The rehearsal now compares actual schema definitions before inserting any backup rows and tests refusal on a mismatch. Application table/sequence fidelity is covered; cluster roles, provider settings and grants outside the versioned application schema are not a physical PostgreSQL cluster backup.

CI subsequently caught physical column ordinals changing after a migration down/up cycle. The schema comparison now sorts columns by name and compares their semantic definition; dropped-column position holes are excluded. Type/default/nullability/constraints and ACL/RLS checks remain enforced. Mismatch diagnostics expose only the names of schema sections, never private definitions.

## Historical reader boundary

The v2–v4 operation fold now lives in a separate read-only interpreter and uses explicitly versioned record shapes. The current v4 entry point delegates to it without changing runtime semantics. A raw historical-row regression fixture checks legacy Canon ownership, one Event shared by two Canons, per-Canon Narrative preservation, composite kind, membership removal and withdrawal at four revisions. It deliberately avoids generating history through the current writer. This prepares old-revision fidelity; it does not introduce v5 writes or assert that the remaining publication/export transition is complete.


The manifest covers all 160 public Narrative records with source/target digests and dispositions: 119 retained identities, 14 additions, 41 retirements, and 22 embedded notes. Eight early reign/founding Events also require removal of editorial `curation` metadata and reader-facing summary replacement. The original 127 Event, 6 Canon and 415 active Relation IDs and all exported membership rows are mapped without rekeying. This is reviewed migration input, not an applied canonical change or final semantic rehearsal.

## v5 canonical candidate and schema rehearsal boundary

`@moirai/contracts/v5` defines the target active records without canonical Event.kind, Canon ownership or Relation applicability memberships. `@moirai/domain/v5` validates the final World union and constructs atomic candidates: Collection withdrawal removes its selection links and owner Narrative while preserving World Events/Relations; Event withdrawal requires explicit dependent cleanup. These modules are staged cutover code, not a second public write API. The live default contract stays v4 until the coordinated A3 transition.

The pure revision-30 transformation passes the shared v5 invariant implementation ([report](a2-v5-snapshot-result.json)): 127 Events, 6 Collections, 415 Relations, 153 memberships, 133 Narratives, 26 derived Composites. It normalizes World/Collection prose and the eight editorial grouping metadata values. The source snapshot remains unchanged. This is an offline candidate check; it is not the restored PostgreSQL migration.

Twelve scenario/operation tests cover shared identity, zero membership, Collection/Composite overlap, World-union contains/temporal conflicts, separate realities with compatible time definitions, unlinked Collection time-display settings, single Narrative ownership, atomic creation, Collection withdrawal, Event dependent cleanup and a deep 10k-event graph. No interactive-read performance claim is made from this canonical validation test. Review found that child counts must count distinct Events rather than relation rows; the derived count now does so.

The versioned schema component `cutovers/010_ip011_collections.ts` is intentionally outside the default live migration registry. Its prepare/content/finalize phases must execute in one explicitly selected migration transaction. It refuses unreviewed Subject handles and refuses dropping old Relation applicability rows before exact archival in Change Operations. Deferred owner/Narrative constraints and same-World membership FKs allow zero memberships while preserving ownership integrity. PostgreSQL integration tests use a disposable database; the original database is not migrated by those tests. Existing live services still use migrations 001–009.

The operator-only `scripts/ip011-v5-db-rehearsal.ts rehearse-v5` now targets an existing database named `ip011_rehearsal_<16 hex>`. It compares full owner backup digests between the live v4 source and that isolated copy, pins the manifest and source Revision, then uses Kysely's transactional migration ledger to apply reviewed content with the v5 schema. It compares the complete v5 candidate after reread, protects all pre-cutover Change Sets/Operations/Revisions and withdrawn Relation facts, archives every old Relation applicability row before removing that table, and checks that the operational source digest has not changed. The clone's new Publication outbox is never processed by a worker. A disposable PostgreSQL integration test exercises the entire transaction and a failing candidate. This runner has no mode for writing to the operational source and is not the A3 live cutover implementation.

PR [#135](https://github.com/neocjmix/moirai/pull/135) merged as `42fe92ddbde56ce1fba1adbfd633d022dc0c73a3`. CI run 35817251858 passed strict typecheck, unit tests, the disposable PostgreSQL transaction rehearsal, production build, mobile regression, dependency audit and secret scan. The Railway worker reached SUCCESS on this commit. A subsequent redeploy reused a worker snapshot; no operator result appeared, so it is **not** evidence of successful restored-DB migration. Railway treated the temporary start-command update as staged. Committing all staged environment changes to run it was rejected by safety review because it could persist a temporary command and include unrelated staged changes. The worker start command was restored to `pnpm --filter @moirai/lachesis-worker start` and verified by a read-only configuration check. The harmless rehearsal DB name variable remains configured. The clone has not been inspected since this attempted run; its v5 rehearsal and A2 exit remain open. Do not retry the rejected environment-wide staged commit indirectly; obtain an explicitly approved, narrowly scoped execution path first.

## Actual restored-database v5 rehearsal (2026-09-23)

The user explicitly approved the Railway environment action after the blocked attempt. The sole pre-existing staged patch reported an empty change list; accepting it cleared pending work but produced no new worker deployment. The Railway authenticated worker console then executed the already-reviewed operator command once against the named isolated restore `ip011_rehearsal_81811c151956e9af`. The command returned `ip011_v5_database_rehearsal` success: migration `010_ip011_collections`, Revision 30→31, Events 127, Collections 6, Narratives 133, Relations 415; `history_unchanged`, `legacy_revision_unchanged`, `withdrawn_relations_unchanged`, and `operational_source_unchanged` all true; `publication_processed` false. This is the real production-snapshot clone, not the disposable CI fixture. The active worker did not point at the clone. The temporary worker start command was reset to `pnpm --filter @moirai/lachesis-worker start`; Railway reports no pending changes and web/API/worker/Postgres latest SUCCESS. The original canonical DB remains v4. This passes the restored-DB schema/content rehearsal gate, **not** the entire A2 exit: v5 transactional public writer, publication/export/read, UI/MCP and scenario end-to-end still require implementation and verification before A3.

PR [#139](https://github.com/neocjmix/moirai/pull/139) merged at `d298c0ba22d349a8a35f6189dd07ee860810f57f` after CI run 35824190847 passed. Its internal-only v5 canonical transaction uses the current policy identity and exact-ID replay before a new-write gate, checks the complete candidate under a World lock, and records revision/history/outbox atomically. Disposable PostgreSQL integration rejects missing/stale policy and incomplete owner Narrative, then commits a new Event+Narrative+membership and withdraws a Collection while keeping all Events and Relations. All three Railway application services are SUCCESS at the merged SHA. It is not an externally advertised v5 authoring endpoint; client references, final caller authorization, publication/read/export, and source provenance wiring remain A2 work.

PR [#140](https://github.com/neocjmix/moirai/pull/140) merged at `48acb0dde1b902a6657716715305d60b27451bcb` after CI run 35826194895 passed. A staged Lachesis boundary now requires authenticated World read/write scope before policy delivery or any commit/replay, refuses an actor in the plan, and injects the trusted server Actor into the internal v5 transaction. This closes the previously noted staged caller-authorization gap; all three Railway app services are SUCCESS at the SHA. It is deliberately not yet wired to Clotho HTTP/MCP or the active canonical DB. Client-ref resolution, operation-level origin refs, v5 transport schema, v5 Revision read/export, Publication and Atropos must be solved before A2 exit. The DB rehearsal success alone does not authorize A3.

PR [#141](https://github.com/neocjmix/moirai/pull/141) merged as `ba3a1255e6d1dadbf8f0d6f33cc87ffa9d9cf0b8` with all CI checks green. It pinned the actual rehearsal and staged-writer evidence without treating A2 as complete. The next staged writer change makes every internal v5 operation carry an origin reference and attributes final history diffs to explicit operations or the Collection withdrawal that implied them. It rejects invalid indices and unexplained diffs instead of recording empty `change_operations.origin_refs`; the isolated PostgreSQL integration asserts both explicit and cascading provenance. This remains internal-only until the external typed authoring contract and idempotent `client_ref` resolution are implemented.

PR [#142](https://github.com/neocjmix/moirai/pull/142) merged as `fff2874313ca59c5330ab08aa54e236532c286fa` after all three CI jobs passed. An initial CI run exposed a stale domain test fixture lacking the newly required source reference; the corrected fixture was pushed and the full CI reran successfully. The next read-only slice reconstructs v5 Revisions from the migration snapshot and later Change Operations, preserving the separate immutable v2–v4 historical interpreter. It is an export/history read, not a bounded interactive query and not yet wired to Publication or Clotho.

PR [#143](https://github.com/neocjmix/moirai/pull/143) merged as `7e3dd733845c056ca7923d1a28e41fe6dbeba1be` after all three CI jobs passed. The disposable PostgreSQL suite reconstructs migration Revision 3 and v5 authored Revisions 4/5 exactly from canonical history while preserving Revision 2 on the legacy reader. A staged draft resolver is being added next: the server derives UUIDv7 IDs from Change Set ID, entity type and `client_ref`, so an uncertain-outcome retry has identical resolved content and stable response mapping. This does not itself constitute a safe external authoring endpoint: Clotho's strict v5 wire schema, version rejection, validation, transport parity and publication/read migration are still required before A2 exit.

PR [#144](https://github.com/neocjmix/moirai/pull/144) merged as `1661ee7c8ca9056720075ffdbb615a5fa20179c4` with all three CI jobs green. The staged Lachesis draft boundary authorizes the World before reference resolution, refuses caller actor/ID mapping, resolves declared entity links with retry-stable IDs, and carries the mapping through the internal transaction's success/replay result. A separate, strict v5 Clotho plan schema and inactive application boundary are staged next: they reject obsolete v4 contract, Canon/Composite fields, missing policy/provenance and caller-owned server fields, and recheck authorization before parsing/commit. None is exposed on live HTTP/MCP/CLI; eventual wiring must switch policy, validator, methods and actual canonical DB in a coordinated release. CI on the first schema commit caught one implicit callback type in the resolver; it was corrected before merge.

PR [#145](https://github.com/neocjmix/moirai/pull/145) merged as `20d0d9678883fdbfcf83ca28c8dfe80d20e92544` after all three CI jobs passed. CI review first caught an implicit callback type, then a formatting issue; both were corrected and the final full run passed. The next content-only v5 offline projection provides one World-owned Event detail, one Narrative per owner, one Relation detail, paged Collection membership/catalog and Event adjacency, plus World-derived Composite child count independent of selection. Its root stays bounded when far-off Events are added. It deliberately cannot publish a served pointer: temporal/spatial/scale indexes, hierarchical manifest, Atropos reader, privacy and full E2E gates are still missing.

The staged World temporal adapter runs the existing relational solver over all World Events, Relations and Time Systems instead of filtering by one Collection. It derives Composite identity from active World `contains`, strips the temporary v4 calculation scope from its return value, and recomputes a World-semantic digest. Tests assert hidden constituent Events and Collection removal do not change temporal truth or shared Event positions. This is an implementation bridge, not a persisted synthetic Canon or a claimed scalable spatial layout. Temporal and content slices are not yet assembled into a complete Publication.

PR [#146](https://github.com/neocjmix/moirai/pull/146) merged as `96a1671e5ed04511a28ada25098654d6ede2d119` after CI run 35890071498 passed all three jobs. It remains inactive to the public reader. The following staged index composes actual content pages into immutable digest references with bounded 128-way branch/leaf/root fanout and checks missing, altered, duplicate, extra and cross-Revision entries offline. Adversarial review found that bounded fanout alone did not yield bounded random access: without key ranges, a reader would still scan every leaf. Branch references now carry disjoint document-key ranges, and a test locates a distant Event among 20,000 documents with one branch per level and one detail read. Its root explicitly says `content-only`; it does not produce `current.json` or publish an Atropos-readable v5 manifest. This is a prerequisite for a bounded complete Publication, not the A2 exit.

PR [#147](https://github.com/neocjmix/moirai/pull/147) merged as `431b6581e464a8cfe5d5ae326cf6575169233203` after CI run 35891791674 passed all three jobs. The follow-on offline temporal detail split pages position evidence, Composite children/descendants/during and duration/span evidence from World-wide truth. Its separate root is `content-and-temporal-detail-only`; it is not a time-range/spatial/scale index and cannot drive a complete viewport query. The data can still have high-degree per-item evidence, so paged list counts alone do not prove a strict byte bound for every detail. A2 full Publication, E2E read and A4 budget gates remain open.

PR [#148](https://github.com/neocjmix/moirai/pull/148) merged as `eabc4f2e86859d5fb1783fbbb55516953d0c63ab` after CI run 35892487646 passed all three jobs. A subsequent inactive direct-Event reader follows the digest-checked key-range path to one content detail and one World temporal position, plus Composite detail when derived children exist. A two-Collection shared Event stays a single Event with one Narrative, and a hidden child still makes its parent Composite. Missing indexed temporal content fails closed instead of returning an apparently complete Event. This is not the public Atropos reader or a viewport query.

PR [#149](https://github.com/neocjmix/moirai/pull/149) merged as `4eec38a345f5264ee50caebc6a080a2bf4dd2db6` after CI run 35893267372 passed all three jobs. The following isolated Fastify v5 HTTP route prototype serves the target policy and strict Change Set through the existing trusted Clotho/Lachesis boundary. It is **not registered on the live v4 application**. An adversarial transport test caught Fastify's default unknown-field removal: it could silently remove caller `actor`/obsolete fields before the strict application validator saw them. The staged route now avoids framework body-schema mutation and validates the untouched request after authentication; tests exercise default Fastify settings, old contract, actor spoofing, wrong-World and valid commit. MCP/CLI parity, DB composition, coordinated cutover and public reading are still open.

PR [#150](https://github.com/neocjmix/moirai/pull/150) merged as `8fc3cafe5625aa7c26d51f9c032e4ca079c9a47f` after CI passed. Web/API/worker reached SUCCESS on the merged SHA. The v5 HTTP transport remains unmounted. The next staged composition passes an actual migrated database to Clotho through the permitted Lachesis database adapter. Its writer explicitly refuses a v4 database lacking migration `010_ip011_collections` before a canonical mutation; isolated PostgreSQL tests cover that refusal, authorized create plus owner Narrative, replay, Revision readback and unchanged source. Local TypeScript, boundary check and unit suite passed; local PostgreSQL is unavailable, so the database assertions require CI. Neither the separate HTTP transport test nor this database test alone proves a single HTTP-to-database end-to-end request. That complete composition, external MCP/CLI parity and Publication/Atropos remain A2 gates.

PR [#151](https://github.com/neocjmix/moirai/pull/151) merged as `dd1f9374750de1bb6be9b9c42bf4111603d4e6c9` after CI run 35938594626 passed all three jobs, including isolated PostgreSQL, strict typecheck, build, mobile regression and secret scan. An initial CI run exposed an invalid upward dependency in a persistence-package test importing Clotho/Lachesis; the cross-layer test was moved to the root integration suite and root-only database test dependency declared. The fixed run proves Clotho/Lachesis/database composition, migration guard, retry and history against a disposable clone. The next test will send an authenticated HTTP request through the already staged route into that same isolated DB; the live app continues serving v4 until coordinated cutover.

PR [#152](https://github.com/neocjmix/moirai/pull/152) merged as `242b7852b395447bb780272fc88b7effd09631b1` after CI run 35939107688 passed all three jobs. The isolated DB integration now sends actual Fastify requests through authenticated v5 policy and Change Set routes into Clotho/Lachesis and the migrated database, checks old-version and unauthenticated refusal, exact retry and Revision readback, and verifies the v4 source remains untouched. This route is still not mounted in the live application. The next staged CLI client can call the separate v5 path but remains unadvertised while the server is v4. MCP parity, Publication/Atropos and cutover gates remain open.

PR [#153](https://github.com/neocjmix/moirai/pull/153) merged as `90faf9438c4c21ce44d3413c39125f0ac8a0076d` after CI run 35939551088 passed all three jobs. The separate v5 CLI path uses existing bearer/redirect/response-budget guards and rejects an incorrectly versioned successful response. Railway initially failed the #152 worker deployment after image build without runtime logs; redeployment succeeded, and #152 web/API/worker were all SUCCESS before #153 merge. The following inactive v5 MCP handler binds policy and commit tools to the same Clotho boundary; unit tests check authorization, tool discovery, strict version rejection. Isolated PostgreSQL replay through this MCP route is added to the root integration suite. The active `/mcp` still advertises v4; OAuth discovery and the coordinated endpoint switch have not happened.

PR [#154](https://github.com/neocjmix/moirai/pull/154) merged as `de6dc8fc3778f07b21960ca019e861b2d312b11a` after CI run 35940178308 passed all three jobs including isolated PostgreSQL HTTP-write/MCP-replay parity. The inactive route requires a trusted actor and strict v5 policy/Change Set, but initially accepted only static credentials. The next change reuses the existing Auth0 issuer/audience/signature/scope verifier for staged MCP and tests a signed read-only token with write refusal. It does not add the staged route to the live application or replace OAuth discovery; that endpoint switch is still gated.

PR [#155](https://github.com/neocjmix/moirai/pull/155) merged as `32f9f9df2d3c92d1891fcc846a6b6a583fd9b071` after CI run 35940643087 passed all three jobs. The inactive v5 MCP now reuses the existing OIDC verifier; a signed JWT with matching issuer/audience/subject and `world:read` can fetch policy but cannot write. The first v5 Publication upload path being staged verifies the complete content-and-temporal rehearsal tree, writes immutable objects with exact-content retry, then writes the manifest root last; it deliberately does not swap `current.json`. A failed immutable conflict refuses the upload. This is still incomplete for public viewport/scale reads and A2 exit.

PR [#156](https://github.com/neocjmix/moirai/pull/156) merged as `e4c02c9c1ae657a0c4df36e7877e1e12ef210813` after CI run 35941057990 passed all three jobs. The staged upload verifies all indexed objects and leaves `current.json` unchanged; retries require exact immutable content. A new bounded Collection reader uses the authenticated index path for Collection detail and one membership page. Two Collections selecting the same Event return the same World Event ID, with no Event detail copy or Collection-specific Event Narrative. It remains offline until a complete manifest and Atropos adapter are built.

PR [#157](https://github.com/neocjmix/moirai/pull/157) merged as `a481c19d6cfe48d917a6d8117b192ce4e17391f8` after CI run 35941530477 passed all three jobs. The inactive Collection reader retrieves one membership page with digest checks and exact owner Narrative; the shared Event remains one ID. The next navigation seam retrieves one catalog page via a World summary and a keyed page, with page bounds and no World-sized transfer. It still cannot produce viewport recommendations or serve the public Atropos contract.

PR [#158](https://github.com/neocjmix/moirai/pull/158) merged as `80fea9a8ad5598e80d5d8af8df90c2d134339423` after CI run 35941929370 passed all three jobs. The staged catalog reads a World summary plus one indexed page and preserves bounded reads. Adversarial review then found that staged v5 MCP required a token even for `initialize` and `tools/list`; a newly connected client could not discover the policy tool before its OAuth token was attached. The follow-on correction permits only side-effect-free discovery and bodyless preflight unauthenticated, while `tools/call` still requires a trusted principal. A real MCP SDK client must initialize and list v5 tools without a token in its regression test. The production v4 MCP continues unchanged.

PR [#159](https://github.com/neocjmix/moirai/pull/159) merged as `cb90f9b8af623250f1fe8ef581d5d1572056d1ca` after CI run 35942454082 passed all three jobs. The real MCP SDK now connects and lists v5 tools unauthenticated, while tool execution returns 401 without a trusted principal. An inactive World adjacency reader is staged next: from one Event, fetch exactly one indexed Relation-ID page, then fetch a selected Relation detail by immutable World identity. The test reaches a neighboring Event outside the active Collection. This does not claim a complete viewport query or a public Atropos reader.

PR [#160](https://github.com/neocjmix/moirai/pull/160) merged as `f4835869879bcf57412d68f8ce4bfc36b9e283ff` after CI run 35942911266 passed all three jobs. World Event adjacency is read by a bounded ID page, with the selected Relation fetched separately; a neighbor outside Collection selection remains reachable. The next inactive Atropos adapter composes the v5 Event, Collection, catalog and adjacency readers with the existing object-store `get` seam. Its input root is still only `content-and-temporal-detail-only` and must be authenticated by a future complete Publication pointer before exposure. No Next.js route is added.

PR [#161](https://github.com/neocjmix/moirai/pull/161) merged as `6b69542eeeafbbcf12ebf2c1da0d63d106437778` after CI run 35943600560 passed all three jobs. The Atropos-internal object-store adapter composes bounded v5 Event, Collection, catalog and adjacency reads while keeping the public v4 route unchanged. A test checks separate Event and Collection Narrative ownership and tampered-object rejection. The next staged reader retrieves a single page of Composite children from World `contains` facts, with no Collection membership precondition. A complete viewport/scale manifest and authenticated serving pointer are still prerequisites for public exposure.

PR [#162](https://github.com/neocjmix/moirai/pull/162) merged as `d32cd9bc03dbf906d3dae6ac0e5738b92eebfb20` after CI run 35944032619 passed all three jobs. The internal Composite reader loads one page of at most 128 child IDs, independent of Collection visibility. Adversarial review found that two distinct World `contains` assertions could name the same child: the canonical distinct-child count and presentation list then disagreed. The projection now deduplicates navigable Event references while retaining both Relation assertions as evidence; a 131-assertion/130-child regression verifies two bounded pages. A subsequent rehearsal gate compares the v5 migration Revision replay against the active target's semantic fingerprint, in addition to checking unchanged v4 history.

PR [#163](https://github.com/neocjmix/moirai/pull/163) merged as `68dc244e558c4dfe2c501b84cec17d5744a8081b` after CI run 35960858967 passed all three jobs, including PostgreSQL migration integration. The isolated rehearsal now rejects a migrated Revision whose persisted-history reconstruction differs from the reviewed target and active state. It continues to verify unchanged legacy history and operational source. This checks one target Revision, not the full owner-full ZIP64 export/import contract. The next staged selected-Collection reader performs a bounded cursor-based merge of membership pages and deduplicates World Event IDs; it still cannot filter by viewport/time/scale or be publicly served.

PR [#164](https://github.com/neocjmix/moirai/pull/164) merged as `7232c09605736e71aeaa8aeb8ee544575fdf237e` after CI run 35961569114 passed all three jobs. An internal cursor-based k-way merge of up to eight selected Collections returns at most 128 sorted unique World Event IDs per page, fetches only requested membership pages and preserves one Event identity for overlap. A 260-Event regression checks three pages, bounded reads, cursor/revision mismatch and rejection of exhausted cursors. This selection union has no temporal range, viewport, zoom/scale or graph neighborhood filter. It is not a complete public read path, Publication pointer or Collection discovery algorithm; A2 and production v5 cutover remain open.

## World-owned viewport index in progress

The staged presentation producer now lays out one World Event graph per Time System without using Collection membership as an ownership or coordinate boundary. Temporal positions come from the World-wide solver; unresolved Events remain explicitly unplaced, and a Composite is a derived region around anchored `contains` children. An offline 20,000-Event sparse fixture builds a 128-fanout spatial hierarchy whose distant local viewport visits at most three spatial nodes; a dense 401-Event viewport returns four continuation pages instead of expanding response size. This is an index traversal result, not a latency/100k claim.

The offline composition bundles World content, temporal details, and all Time System spatial nodes into one hierarchical SHA-256 Publication tree. The spatial manifest's revision, World, Time System, temporal digest and placed+unplaced count are checked. An internal Atropos adapter loads the manifest and each node through digest-verified reads with an aggregate 256-object ceiling. A shared Event has one geometry and one Event Narrative; tampering with a spatial node fails closed. The root is deliberately `content-temporal-and-spatial-staged`, not a complete serving pointer. There is still no selected-Collection intersection, scale-specific shard, signed cursor, public route, authoring read/import/export parity or UX integration. The current v4 `current.json` is untouched. These are mandatory A2/A4 gates, not merely tuning tasks.

The next internal slice adds exact Event→Collection inverse postings, derived from the same canonical membership rows and indexed by the outer digest tree. The viewport adapter probes at most eight active Collections for a bounded number of raw spatial candidates per page, returns one World shape for an Event selected by multiple Collections, and pins continuation to the selection digest. A dense selection test verifies all pages and stale-selection refusal; empty selection returns no World scan. This is still not a public serving path. Scale shards, time/neighbor filters, pointer authentication, cursor integrity against client forgery, and total payload/latency/100k measurement remain open.

PR [#166](https://github.com/neocjmix/moirai/pull/166) merged as `5321c842cbfddf0ba01380eb05d3d186aa75916d` after CI run 35986925430 passed all three jobs (including mobile WebKit). The first run caught a package.json trailing comma before installation; the second run caught a package declaration return type not surfaced by the root-only typecheck. Both were corrected and the complete CI reran green. The active service still serves v4; this merge does not satisfy A2 exit.

A separate inactive ZIP64 v5 content package uses `collections` and Collection/Event membership sections, with no Canon applicability or Event-owned Collection field. Its digest reader and v5 World invariant validator round-trip a shared Event, an unselected Event, a World `contains` Relation and exactly one Narrative per owner. The historical v1–v3 content reader continues to reject this new format rather than reinterpret old bytes. This is bounded in-memory content-package rehearsal, not owner-full history, large streaming export, clone import authorization or live `world.export` cutover; those remain A2 gates.

An operator-only read-only command is staged to compare source and restored clone Revision 30, compare the clone's v5 Revision 31 historical replay with its active rows, and round-trip a v5 content ZIP64 package from the actual clone. It refuses an unexpected clone name/database, logs only counts/fingerprint/size, and has no S3 or import write path. Local fixture tests are not evidence this command has run against the actual clone; its real result must be recorded separately.

PR [#167](https://github.com/neocjmix/moirai/pull/167) merged as `fa03dd80f6609dd5224517522bd00b7c23df43f3` after CI run 35987614158 passed all three jobs. Merge/build success alone was not evidence of an actual clone export; the separate console execution below provides that evidence.

The actual clone command then ran once in the connected Railway worker console at deployed SHA `fa03dd80f6609dd5224517522bd00b7c23df43f3`, explicitly targeting `ip011_rehearsal_81811c151956e9af`. Its sanitized result was `ip011_v5_content_export_rehearsal`: source and clone Revision 30 matched, clone Revision 31 history replay matched active rows, v5 ZIP64 content package round-tripped, fingerprint `sha256:e7a14059bbf9973af2c5428fa28e1d6e6224023d5e44054606ae6ef83a6ac0f0`, 467652 package bytes; Events 127, Collections 6, Relations 415, memberships 153, Narratives 133. The command has no write path; no source DB, schema, S3 or pointer was changed. This passes content-only actual-data fidelity, **not** owner-full history export, clone import or public v5 serving.

A target v5 pointer authenticator and new `/graph/v5/viewport` server route are staged separately. The route caps input bytes, validates selection/cursor shape, pins the read to the v5 pointer's root digest, and refuses v4 or `content-temporal-and-spatial-staged` roots. A test-only synthetic `complete` root demonstrates the read composition and tamper refusal, but **no production builder emits `complete`**, no v5 pointer is written, and the active graph remains v4. Scale/time/neighborhood completeness, public parity and UX are still required before claiming serving readiness.

Adversarial review found that an append-only rehearsal manifest and its later final manifest could collide on the same Revision/object key; adding shards would also rewrite index nodes under the same keys. The rehearsal index/root now live under `v5/staging/{completeness}/`, while a future proven serving tree is reserved under `v5/complete/`. Repeated progressive staging in one Revision cannot overwrite prior immutable artifacts, and a pointer must name the complete root and its digest. A unit test uploads successive content, temporal and spatial rehearsal trees at one Revision without changing `current.json`. The complete producer remains deliberately absent.

PR [#168](https://github.com/neocjmix/moirai/pull/168) merged as `23cedf36e19c7beff3201587159e9f179ee9599d` after CI run 35989815619 passed typecheck/unit/PostgreSQL/build, mobile regression and secret scan. Railway web/API/worker reached SUCCESS at that SHA. The v5 pointer guard and viewport route remain inert with the current v4 pointer. This merge does not claim a completed Publication, browser migration or A2 exit.

A follow-on `/graph/v5/read` detail/navigation route uses the same complete-pointer/digest gate for one Event, Collection membership page, Collection catalog page, adjacency page, Relation or Composite child page. Requests have a byte limit; a single response has an 80-object-read and 256 KiB ceiling. The synthetic complete-root test proves that two Collections resolve the same World Event and its sole owner Narrative. This is an inactive transport seam, not an actual complete-tree proof or the final graph UI.

PR [#169](https://github.com/neocjmix/moirai/pull/169) merged as `ee003fa5dceb83fa552c0799db394c5dfe68c90a` after CI run 35990546276 passed all three jobs. The follow-on operator command `ip011-v5-publication-rehearsal.ts rehearse-publication` reads only the explicitly named migrated clone, checks Revision replay against active state, builds and verifies an in-memory staged content/temporal/spatial digest tree, and pages every Collection selection in every Time System against the canonical memberships of placed Events. It reports build time, object/byte counts and maximum per-page object reads without publishing to S3. A local shared-Event/unplaced-Event fixture passes; the actual clone result remains to be executed and recorded.
